# OpenMontage - Core Dependencies
pyyaml>=6.0
pydantic>=2.0
jsonschema>=4.20
python-dotenv>=1.0
Pillow>=10.0
numpy>=1.24
requests>=2.31
google-auth>=2.0       # service-account auth for Google TTS + Imagen (Vertex AI)
google-genai>=1.0.0    # Gemini Developer API clients for Lyria, Veo, and Gemini Omni
openai>=2.44.0         # Videos API support for Sora 2

# Backlot - the living storyboard (local board server)
fastapi>=0.110
uvicorn>=0.29
watchfiles>=0.21

# Local open-source tooling (CPU / Apple Silicon MPS)
yt-dlp>=2026.0         # source ingest (video_downloader)
youtube-transcript-api>=1.0  # transcript_fetcher
rembg>=2.0             # background removal (bg_remove)
realesrgan>=0.3        # AI upscaling (upscale)
gfpgan>=1.3            # face restoration (face_restore)
manim>=0.19            # math animation (math_animate)
piper-tts>=1.2         # local TTS (piper_tts)
pykokoro[cpu]>=0.8     # Kokoro-82M local TTS (kokoro_tts)
chatterbox-tts>=0.1    # SoTA open-source voice cloning (chatterbox_tts)
demucs>=4.0            # music stem separation (stem_separator)
torch>=2.0             # local inference (clip_search, corpus_builder, upscale)
torchvision>=0.20,<0.28  # pinned below 0.28 for basicsr/realesrgan compat
transformers>=4.40     # clip_search, corpus_builder
